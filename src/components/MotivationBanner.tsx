export function MotivationBanner() {
  return (
    <section
      aria-label="System banner"
      className="-mx-4 border-b border-[var(--border)] px-4 py-4 text-center sm:-mx-6 sm:px-6 lg:-mx-8 lg:px-8"
    >
      <p className="font-mono text-[11px]/[1.75] font-bold uppercase tracking-[0.32em] text-[var(--foreground)] sm:text-xs/[1.75]">
        Reset. Restart. Refocus. As many times as you need to. Just don’t quit ‼️
      </p>
    </section>
  );
}
